// Автор: Кожемякин Даниил Сергеевич, группа: 231-3213. Целевой интерфейс паттерна «Адаптер».

namespace ClockAdapter;

/// <summary>
/// Целевой интерфейс (Target) паттерна «Адаптер».
/// Описывает ожидаемый клиентом способ получения времени в цифровом виде.
/// </summary>
public interface IDigitalTimeDisplay
{
    /// <summary>
    /// Возвращает время в формате «ЧЧ : ММ» (12-часовой циферблат, часы 01–12).
    /// </summary>
    string GetDigitalTime();
}
